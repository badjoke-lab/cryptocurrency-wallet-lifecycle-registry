import type { WalletEvidence } from '../lib/types'
export default function EvidenceList({rows}:{rows:WalletEvidence[]}){return <div className="evidence-list">{rows.map(source=><article className="evidence-row" key={source.id}><div><a className="strong-link" href={source.url} target="_blank" rel="noreferrer">{source.title}</a><div className="muted small">{source.publisher}{source.published_at?` · ${source.published_at}`:''}</div></div><div className="evidence-meta"><span>{source.source_type}</span><span>{source.reliability}</span>{source.is_primary&&<span>primary</span>}</div></article>)}</div>}
