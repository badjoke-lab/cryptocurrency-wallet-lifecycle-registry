name: Compute Wave 03 Thin History 7

on:
  push:
    branches:
      - research/coverage-recovery-wave-03-thin-history-7

permissions:
  contents: write

jobs:
  compute:
    if: github.actor != 'github-actions[bot]'
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/compute-wave-03-thin-history-7.mjs
      - name: Commit queue
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add data-staging/coverage/coverage-recovery-wave-03-thin-history-7.json
          git commit -m "Compute Wave 03 thin-history batch 7 queue"
          git push
