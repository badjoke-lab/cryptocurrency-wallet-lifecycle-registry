# Bootstrap verification

Local canonical validation before repository publication passed with:

- 12 entities
- 40 products
- 44 events
- 56 evidence records

The repository CI repeats record validation and runs the complete Next.js static build on pull requests and on `main`.

UI direction: CYA-derived editorial registry layout on a black palette, with semantic lifecycle/status/severity colors. The Support page temporarily shares the current HEI donation addresses.
